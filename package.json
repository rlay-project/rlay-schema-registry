{
  "name": "@rlay/schema-registry",
  "version": "1.0.0",
  "description": "Remote registry client for your rlay schema via redis",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha --exit"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rlay/rlay-client-lib": "^0.2.0-pre.9",
    "assert": "^2.0.0",
    "ioredis": "^4.14.0",
    "ioredis-mock": "^4.16.3",
    "mocha": "^6.2.0"
  },
  "dependencies": {
    "check-types": "^9.0.1"
  }
}
